Form
{
    name: ""

    fields: []                          
    components: []                      

    timed: true/false           
    timedFields: []                     

    layouts: {}
}

Field
{
    name: ""                    Identifier
    type: str/int/dec/bool/tags/imgs
        str                     String
            maxLength           
        int                     Integer
            min                 Null if no min
            max                 Null if no max
            step
        dec                     Decimal/Float
            min
            max
            step
            roundDigits
        bool                    Boolean
        tags
            options
            allowMultiple       true/false for multi select
        imgs
            allowMultiple
    value:
    default:
}

TimedField

Action
[]

Component
{
    uuid exists just for editing purposes
    name:
    type:
    value: expression/field                     Expression if display only, otherwise its connected to a field
    showIf: expression
    layouts: {
        portrait: {x,y,z,width,height}          As percentages
        landscape: {x,y,z,width,height}         As percentages
    }
    locked: expression                          Let users change value
    type: 
        Label                                   (can be image/text)
            image
            labelText
        Button
            onTap
            onDown
            onUp
            image
            labelText
        Dropdown/TagToggle                      (tags)
            onChange
            labelText
        ShortText/LongText/NumberInput
            onChange
            labelText
        FormPage
            components
        Photo
            labelText
        Draw                                    (draw on field, like for auto routines)
            image                               Background image - can be expression (like for positions)
            labelText
    fontSize: options are in code in ComponentSettings.vue
    fontColor: options are in code in ComponentSettings.vue
    bgColor: options are in code in ComponentSettings.vue
}

Expression Examples
[["var", "a", "=", "const", "5"], "and", true]

Expression Things:
X Add X
X Subtract X
X Multiply X 
X Divide X
X String Combine X



Expression Constants
Alliance